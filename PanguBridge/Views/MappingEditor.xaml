<UserControl x:Class="PanguBridge.Views.MappingEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Button Mapping" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBox DockPanel.Dock="Top" Style="{StaticResource SelectableText}" Margin="0,0,0,4" IsReadOnly="True"
                 Text="Each physical button drives the vJoy button slot number below (1-19). Leave blank to decode the button but not send it to vJoy." />
        <TextBox DockPanel.Dock="Top" Style="{StaticResource SelectableText}" Margin="0,0,0,12" IsReadOnly="True"
                 Foreground="{DynamicResource SubtleForeground}"
                 Text="For this mapping to work, the vJoy device itself must be configured with exactly 19 buttons, the X/Y/Z/Rx/Ry/Rz axes, and 1 continuous POV hat (not 4-direction/discrete) - see the device settings above." />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="SaveButton" Content="Save" Width="100" Click="SaveButton_OnClick" Margin="0,0,8,0" />
            <Button x:Name="ResetButton" Content="Reset to Default" Width="140" Click="ResetButton_OnClick" />
        </StackPanel>

        <DataGrid x:Name="MappingGrid" Margin="24,0" AutoGenerateColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Physical Button" Binding="{Binding SourceButton}" IsReadOnly="True" Width="200" />
                <DataGridTextColumn Header="vJoy Button (1-19, blank = unmapped)" Binding="{Binding VJoyButtonText}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
