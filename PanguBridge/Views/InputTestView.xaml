<UserControl x:Class="PanguBridge.Views.InputTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Loaded="InputTestView_OnLoaded" Unloaded="InputTestView_OnUnloaded">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="16">
        <TextBlock Text="Input Test" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" />

        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,8">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="TestLeftGripButton" Content="Test Left Grip"
                            Margin="0,0,8,0" Click="TestLeftGripButton_OnClick" />
                    <Button x:Name="TestRightGripButton" Content="Test Right Grip"
                            Margin="0,0,8,0" Click="TestRightGripButton_OnClick" />
                    <Button x:Name="TestLeftTriggerButton" Content="Test Left Trigger"
                            Margin="0,0,8,0" Click="TestLeftTriggerButton_OnClick" />
                    <Button x:Name="TestRightTriggerButton" Content="Test Right Trigger"
                            Click="TestRightTriggerButton_OnClick" />
                </StackPanel>
                <TextBox Style="{StaticResource SelectableText}" Margin="0,8,0,0" IsReadOnly="True"
                         Foreground="{DynamicResource SubtleForeground}" FontStyle="Italic"
                         Text="Test intensity is scaled by the Rumble Intensity caps set in Options." />
            </StackPanel>
        </Border>

        <TextBox Style="{StaticResource SelectableText}" Margin="0,0,0,8" IsReadOnly="True"
                 Foreground="{DynamicResource SubtleForeground}" FontStyle="Italic"
                 Text="Values may lag slightly behind the real controller." />

        <DataGrid x:Name="InputGrid" Margin="24,0" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Input" Binding="{Binding Name}" Width="160" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                            <Setter Property="Background" Value="SeaGreen" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </StackPanel>
    </ScrollViewer>
</UserControl>
