<Window x:Class="PanguBridge.Views.UpdateCheckDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Pangu Bridge" Height="190" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackground}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" Foreground="{DynamicResource Foreground}" />

            <StackPanel x:Name="VersionInfoPanel" Margin="0,16,0,0">
                <TextBlock Foreground="{DynamicResource SubtleForeground}">
                    <Run Text="Installed version: " /><Run x:Name="LocalVersionRun" />
                </TextBlock>
                <TextBlock x:Name="LatestVersionText" Foreground="{DynamicResource SubtleForeground}">
                    <Run Text="Latest release: " /><Run x:Name="LatestVersionRun" />
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="ActionButton" Margin="0,0,8,0" Padding="10,4" Click="ActionButton_OnClick" />
            <Button x:Name="CloseButton" Content="Close" Padding="10,4" Click="CloseButton_OnClick"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
